import Foundation

/// Package-wide version metadata.
public enum AFMServer {
    /// Semantic version of afm-server. Keep in sync with the git tag (`vX.Y.Z`).
    public static let version = "0.1.0"
}
